export interface ApiRequestBayload {
  // Define the properties you expect in the API request body
    user: {
      username: string;
      email: string;
      password: string;
    };
  
  
  
}
